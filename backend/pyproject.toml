[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
    "uvicorn>=0.40.0",
]
